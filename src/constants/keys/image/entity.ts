export enum ENTITY_IMAGE_KEYS {
  PLAYER = "player",
  SPIKE = "spike",
  COIN = "coin",
  JUMPER = "jumper",
  JUMPER_ACTIVE = "jumper-active",
  CHECKPOINT = "checkpoint",
  CHECKPOINT_PRESSED = "checkpoint-pressed",
}

export enum ENTITY_SPRITE_KEYS {
  PLAYER = "player",
  COIN = "coin",
}
