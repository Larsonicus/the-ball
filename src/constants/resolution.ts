import { ScaleMode } from "@/types";

const DEFAULT_WIDTH: number = 480;
const DEFAULT_HEIGHT: number = 270;
const MAX_WIDTH: number = DEFAULT_WIDTH * 1.5;
const MAX_HEIGHT: number = DEFAULT_HEIGHT * 1.5;
const SCALE_MODE: ScaleMode = "SMOOTH";

export { DEFAULT_HEIGHT, DEFAULT_WIDTH, MAX_HEIGHT, MAX_WIDTH, SCALE_MODE };
